// load matrix
$('#compatibility-matrix').replaceWith('<%=
  j render partial: 'matrix', locals: { current_licence: (!@compatibility.nil? ? @compatibility.licence : nil),
                                        licences: @licences } %>');

// initialize
CompatibilityMatrix.initializeMatrices();